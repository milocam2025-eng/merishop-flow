"use client";
import { FormEvent,useState } from "react";
import { useRouter } from "next/navigation";
import { createClient } from "@/lib/supabase/client";
export default function LoginPage(){ const [email,setEmail]=useState(""); const [password,setPassword]=useState(""); const [message,setMessage]=useState(""); const router=useRouter(); async function login(e:FormEvent<HTMLFormElement>){e.preventDefault();setMessage("Ingresando...");const {error}=await createClient().auth.signInWithPassword({email,password});if(error){setMessage(error.message);return;}router.push("/dashboard");} async function register(){setMessage("Creando cuenta...");const {error}=await createClient().auth.signUp({email,password});setMessage(error?error.message:"Cuenta creada. Revisa tu correo si Supabase solicita confirmación.");} return <main className="login-page"><section className="login-card"><div className="logo">M</div><h1>MeriShop Flow Pro</h1><p>Clientes, pedidos, pagos, inventario y envíos.</p><form onSubmit={login}><label>Correo<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Contraseña<input type="password" minLength={6} required value={password} onChange={e=>setPassword(e.target.value)}/></label><button type="submit">Iniciar sesión</button><button type="button" className="secondary" onClick={register}>Crear cuenta</button></form><small>{message}</small></section></main>; }